# @intelligent-farming/lorawan-credential-format

Normalize, validate, and convert LoRaWAN credential strings.

Handles the small, repetitive concerns that every LoRaWAN tool reimplements
and frequently gets wrong:

- Stripping cosmetic byte separators (`-`, `:`, `_`, whitespace) from labels
- Uppercasing hex
- Validating length and character set for the standard credential types
  (DevEUI / JoinEUI / AppKey / NwkKey / DevAddr)
- Converting between hex strings and `Uint8Array` (both directions)
- Swapping byte order — LoRaWAN labels print MSB-first but the air protocol
  transmits EUIs LSB-first, and a packet capture or join-server log will
  surface the LSB form. [swapByteOrder](#swapbyteorder) bridges the two.

Every function is isomorphic — there are no Node-only dependencies, so the
module works unchanged in browsers, edge runtimes, and Node.

## Classes

### CredentialFormatError

Thrown by the `parse*` family of functions when an input cannot be normalized
to a valid hex string of the expected length.

#### Extends

- `Error`

#### Constructors

##### Constructor

> **new CredentialFormatError**(`kind`, `raw`, `detail`): [`CredentialFormatError`](#credentialformaterror)

###### Parameters

###### kind

[`CredentialKind`](#credentialkind) \| `"hex"`

###### raw

`string`

###### detail

`string`

###### Returns

[`CredentialFormatError`](#credentialformaterror)

###### Overrides

`Error.constructor`

#### Properties

##### kind

> `readonly` **kind**: [`CredentialKind`](#credentialkind) \| `"hex"`

Which credential kind the parser was expecting.

##### message

> **message**: `string`

###### Inherited from

`Error.message`

##### name

> **name**: `string`

###### Inherited from

`Error.name`

##### raw

> `readonly` **raw**: `string`

The original input (after `String(input)` coercion but before any trimming).

##### stack?

> `optional` **stack?**: `string`

###### Inherited from

`Error.stack`

##### stackTraceLimit

> `static` **stackTraceLimit**: `number`

The `Error.stackTraceLimit` property specifies the number of stack frames
collected by a stack trace (whether generated by `new Error().stack` or
`Error.captureStackTrace(obj)`).

The default value is `10` but may be set to any valid JavaScript number. Changes
will affect any stack trace captured _after_ the value has been changed.

If set to a non-number value, or set to a negative number, stack traces will
not capture any frames.

###### Inherited from

`Error.stackTraceLimit`

#### Methods

##### captureStackTrace()

> `static` **captureStackTrace**(`targetObject`, `constructorOpt?`): `void`

Creates a `.stack` property on `targetObject`, which when accessed returns
a string representing the location in the code at which
`Error.captureStackTrace()` was called.

```js
const myObject = {};
Error.captureStackTrace(myObject);
myObject.stack;  // Similar to `new Error().stack`
```

The first line of the trace will be prefixed with
`${myObject.name}: ${myObject.message}`.

The optional `constructorOpt` argument accepts a function. If given, all frames
above `constructorOpt`, including `constructorOpt`, will be omitted from the
generated stack trace.

The `constructorOpt` argument is useful for hiding implementation
details of error generation from the user. For instance:

```js
function a() {
  b();
}

function b() {
  c();
}

function c() {
  // Create an error without stack trace to avoid calculating the stack trace twice.
  const { stackTraceLimit } = Error;
  Error.stackTraceLimit = 0;
  const error = new Error();
  Error.stackTraceLimit = stackTraceLimit;

  // Capture the stack trace above function b
  Error.captureStackTrace(error, b); // Neither function c, nor b is included in the stack trace
  throw error;
}

a();
```

###### Parameters

###### targetObject

`object`

###### constructorOpt?

`Function`

###### Returns

`void`

###### Inherited from

`Error.captureStackTrace`

##### prepareStackTrace()

> `static` **prepareStackTrace**(`err`, `stackTraces`): `any`

###### Parameters

###### err

`Error`

###### stackTraces

`CallSite`[]

###### Returns

`any`

###### See

https://v8.dev/docs/stack-trace-api#customizing-stack-traces

###### Inherited from

`Error.prepareStackTrace`

## Type Aliases

### CredentialKind

> **CredentialKind** = `"devEui"` \| `"joinEui"` \| `"appKey"` \| `"nwkKey"` \| `"devAddr"`

Canonical LoRaWAN credential names, keyed by their hex length in characters.

## Variables

### CREDENTIAL\_LENGTHS

> `const` **CREDENTIAL\_LENGTHS**: `Record`\<[`CredentialKind`](#credentialkind), `number`\>

Hex-character length of each [CredentialKind](#credentialkind).

***

### parseAppKey

> `const` **parseAppKey**: (`input`) => `string`

Normalize and validate an AppKey. Throws [CredentialFormatError](#credentialformaterror) on failure.

#### Parameters

##### input

`string`

#### Returns

`string`

***

### parseDevAddr

> `const` **parseDevAddr**: (`input`) => `string`

Normalize and validate a DevAddr. Throws [CredentialFormatError](#credentialformaterror) on failure.

#### Parameters

##### input

`string`

#### Returns

`string`

***

### parseDevEui

> `const` **parseDevEui**: (`input`) => `string`

Normalize and validate a DevEUI. Throws [CredentialFormatError](#credentialformaterror) on failure.

#### Parameters

##### input

`string`

#### Returns

`string`

***

### parseJoinEui

> `const` **parseJoinEui**: (`input`) => `string`

Normalize and validate a JoinEUI / AppEUI. Throws [CredentialFormatError](#credentialformaterror) on failure.

#### Parameters

##### input

`string`

#### Returns

`string`

***

### parseNwkKey

> `const` **parseNwkKey**: (`input`) => `string`

Normalize and validate an NwkKey. Throws [CredentialFormatError](#credentialformaterror) on failure.

#### Parameters

##### input

`string`

#### Returns

`string`

***

### tryParseAppKey

> `const` **tryParseAppKey**: (`input`) => `string` \| `null`

Lenient variant of [parseAppKey](#parseappkey). Returns `null` instead of throwing.

#### Parameters

##### input

`string`

#### Returns

`string` \| `null`

***

### tryParseDevAddr

> `const` **tryParseDevAddr**: (`input`) => `string` \| `null`

Lenient variant of [parseDevAddr](#parsedevaddr). Returns `null` instead of throwing.

#### Parameters

##### input

`string`

#### Returns

`string` \| `null`

***

### tryParseDevEui

> `const` **tryParseDevEui**: (`input`) => `string` \| `null`

Lenient variant of [parseDevEui](#parsedeveui). Returns `null` instead of throwing.

#### Parameters

##### input

`string`

#### Returns

`string` \| `null`

***

### tryParseJoinEui

> `const` **tryParseJoinEui**: (`input`) => `string` \| `null`

Lenient variant of [parseJoinEui](#parsejoineui). Returns `null` instead of throwing.

#### Parameters

##### input

`string`

#### Returns

`string` \| `null`

***

### tryParseNwkKey

> `const` **tryParseNwkKey**: (`input`) => `string` \| `null`

Lenient variant of [parseNwkKey](#parsenwkkey). Returns `null` instead of throwing.

#### Parameters

##### input

`string`

#### Returns

`string` \| `null`

## Functions

### fromBytes()

> **fromBytes**(`bytes`): `string`

Convert a `Uint8Array` to an uppercase hex string with no separators.
Round-trips cleanly with [toBytes](#tobytes).

#### Parameters

##### bytes

`Uint8Array`

#### Returns

`string`

***

### inferKind()

> **inferKind**(`input`): [`CredentialKind`](#credentialkind) \| `undefined`

Infer which credential kind a normalized hex string could be, based on
length alone. Returns `undefined` when the length doesn't match any known
kind, or when the kind is ambiguous (e.g. 16 chars matches both DevEUI and
JoinEUI — in that case `'devEui'` is preferred since it's the more common
caller intent).

#### Parameters

##### input

`string`

#### Returns

[`CredentialKind`](#credentialkind) \| `undefined`

***

### isAppKey()

> **isAppKey**(`input`): `boolean`

True when `input`, after normalization, is a 32-char hex AppKey.

#### Parameters

##### input

`string`

#### Returns

`boolean`

***

### isDevAddr()

> **isDevAddr**(`input`): `boolean`

True when `input`, after normalization, is an 8-char hex DevAddr.

#### Parameters

##### input

`string`

#### Returns

`boolean`

***

### isDevEui()

> **isDevEui**(`input`): `boolean`

True when `input`, after normalization, is a 16-char hex DevEUI.

#### Parameters

##### input

`string`

#### Returns

`boolean`

***

### isHex()

> **isHex**(`input`, `length?`): `boolean`

Test whether a string is valid hex after [normalize](#normalize).

#### Parameters

##### input

`string`

The string to test.

##### length?

`number`

Optional required length in hex characters. If omitted, any
              non-empty even-length hex string passes.

#### Returns

`boolean`

***

### isJoinEui()

> **isJoinEui**(`input`): `boolean`

True when `input`, after normalization, is a 16-char hex JoinEUI / AppEUI.

#### Parameters

##### input

`string`

#### Returns

`boolean`

***

### isNwkKey()

> **isNwkKey**(`input`): `boolean`

True when `input`, after normalization, is a 32-char hex NwkKey (LoRaWAN 1.1.x).

#### Parameters

##### input

`string`

#### Returns

`boolean`

***

### normalize()

> **normalize**(`input`): `string`

Strip cosmetic separators from a hex string and uppercase it.

Removes ASCII whitespace, `-`, `:`, and `_` — the four characters vendors
commonly insert between bytes on labels and in human-typed input. Does
**not** validate that the result is hex; that's the job of [isHex](#ishex)
and the type-specific predicates.

#### Parameters

##### input

`string`

#### Returns

`string`

#### Example

```ts
normalize('a8-40-41-03-56-60-e3-aa') // → 'A84041035660E3AA'
normalize('A8:40:41:03:56:60:E3:AA') // → 'A84041035660E3AA'
normalize(' a84041 035660 e3aa ')    // → 'A84041035660E3AA'
```

***

### swapByteOrder()

> **swapByteOrder**(`input`): `string`

Reverse the byte order of a hex string.

LoRaWAN devices print their EUIs MSB-first on labels (`A84041035660E3AA`)
but transmit them LSB-first on the air. When you read a packet capture or
join-server debug log, the same EUI appears as `AAE36056034140A8` — that's
not corruption, it's the LSB form. Use this to translate between the two.

Works on any hex of even length. The input is normalized first, so cosmetic
separators are tolerated.

#### Parameters

##### input

`string`

#### Returns

`string`

#### Example

```ts
swapByteOrder('A84041035660E3AA') // → 'AAE36056034140A8'
```

#### Throws

[CredentialFormatError](#credentialformaterror) when the input isn't valid even-length hex.

***

### toBytes()

> **toBytes**(`input`): `Uint8Array`

Convert a hex string to a `Uint8Array`. Accepts any of the cosmetic
separator forms — `-`, `:`, `_`, whitespace — handled by [normalize](#normalize).

#### Parameters

##### input

`string`

#### Returns

`Uint8Array`

#### Throws

[CredentialFormatError](#credentialformaterror) when the input contains non-hex
        characters or has an odd hex length after normalization.
